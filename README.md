# 3ilm

[![Open in Bolt](https://bolt.new/static/open-in-bolt.svg)](https://bolt.new/~/sb1-gaxbmrym)

## OpenRouter AI Chatbot Setup

1. Copy `.env.example` to `.env`.
2. Open `.env` and add your OpenRouter API key:
   ```
   OPENROUTER_API_KEY=your_openrouter_api_key_here
   ```
3. Install dependencies if you haven't already:
   ```bash
   npm install
   ```
4. Start the backend proxy:
   ```bash
   npm run server
   ```
5. In another terminal, start the frontend:
   ```bash
   npm run dev
   ```

Then open the app in your browser. The chat button will now use OpenRouter with fallback models.
"# iqraa" 
"# iqraa" 
# repository
